# v1.0 Tier 4: onboarding (notebooks, operator-chooser guide, install page)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Close the v1.0 onboarding gaps from the readiness assessment: give KalmanFilter and BayesianRegression a worked notebook, give the pairs/regression family (RollingBeta/RollingSpread/RollingAlpha/RollingOU) one, fill the signal notebook's despiker/filter gaps, add a cross-catalog "how to choose an operator" guide, and add a real installation/prerequisites page.

**Architecture:** Two new executed notebooks (17, 18) plus edits to an existing one (04), all matplotlib + synthetic seeded data; one new top-level `kind: guide` Markdown page; one new installation page wired into README and the docs index. Notebooks are validated by executing them at build time (myst-nb `nb_execution_mode="auto"`, 120s timeout, raise-on-error).

**Tech Stack:** Jupyter (.ipynb via nbformat), matplotlib (Agg headless, NO plotly in notebooks), numpy, screamer. Sphinx + myst-nb. Build entry: `make docs`.

## Global Constraints

- **No em-dashes** anywhere (ASCII hyphens only), prose, comments, or markdown cells.
- **Follow the docs writing style** (the user's finalized guide): lead with the point, introduce a term before relying on it, say it once, plain language, short readable sentences. NO document-level roadmap opener ("this notebook walks through X then Y"); state what the thing does in one plain sentence. NO closing "Summary/Takeaways/Conclusion/Caveats" cell; a notebook ends when its content ends. NO "batch equals streaming" / "batch == stream" / "so the same code runs live" claims in notebooks (that property is stated once elsewhere, not per-notebook). NO `assert_allclose` / batch-vs-stream / parity-with-pandas proof cells; notebooks show USE, not proofs. Plain descriptive headings, not slogans.
- **Notebooks: matplotlib only**, no plotly. Synthetic seeded data (`np.random.default_rng(<seed>)`). No network. Define a small `show()`/plotting helper in the setup cell if helpful, as notebook 04 does. Keep total execution under ~30s (budget is 120s).
- **Commit notebooks with outputs cleared** (no stored outputs), matching notebook 04, so `make docs` executes and validates them. Before committing, VERIFY execution locally with `poetry run jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=120 <nb> --stdout > /dev/null` (or equivalent) and confirm zero errors, THEN clear outputs.
- **All operator logic stays in screamer** (C++ core); notebooks and guides only call existing operators. Do not reimplement any operator in numpy in a notebook. If a notebook needs a reference value, compute it with the screamer operator, not a hand-rolled version.
- Do NOT edit version files. Do NOT push. Commit after each task with the footer:

  ```
  Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_018q4wFbrQaLrzUFc1H5NpJx
  ```

## Reference facts (from the doc inventory)

- Notebooks live in `docs/notebooks/`, numbered `01-...` to `16-...`. New ones are `17-...` and `18-...`.
- Registration for a notebook is in `docs/notebooks/index.md`: (a) a grid card, and (b) a bare filename line in the hidden toctree at the bottom. Grid card syntax:
  ```
  :::{grid-item-card} <Title>
  :link: <filename-without-extension>
  :link-type: doc
  <One-line description.>
  :::
  ```
- Notebook 04 (`04-signal-processing.ipynb`) has NO stored outputs and executes at build. Its setup cell imports numpy + matplotlib + screamer ops, uses `rng = np.random.default_rng(7)`, defines a `show()` helper.
- A `kind: guide` page has frontmatter `name / title / kind: guide / short / topics` only (no inputs/outputs/parameters), body is prose + markdown tables, ends with `<!-- HELP_END -->`. Example: `docs/functions_fin/choosing_a_backtest_engine.md`.
- Top-level guide pages (usage.md, polymorphic_api.md, nan_and_warmup.md) live in `docs/` root and are listed in `docs/index.rst` toctrees.
- Install text today: `README.md` (a bare ```` ```bash / pip install screamer ```` block) and `docs/index.rst` (a `.. code-block:: console` with `pip install screamer`). Python floor: `requires-python = ">=3.11"`. Sole runtime dependency: `pybind11>=2.9`. Extras: `[test]` (pytest, numpy>=2.0, scipy, pandas, PyYAML), `[docs]` (sphinx, myst-nb, matplotlib, numpy, ...).
- Operator signatures to confirm from each op's doc page frontmatter before use: `KalmanFilter(process_var=, observation_var=, ...)` (1-in, filtered estimate; posterior variance is NOT currently exposed - do not rely on it); `BayesianRegression(...)` outputs `[slope, intercept, pred_mean, pred_std]` (slope-first as of the API-hardening pass); `RollingBeta`, `RollingSpread`, `RollingAlpha` (2-input pair ops), `RollingOU` (mean-reversion params, `output=` STRING modes "mrr"/"mean"/"relmean"/"std"). READ each page under docs/functions_*/ for exact constructor args and input/output counts.

---

### Task 1: Notebook 17 - Filtering and forecasting with uncertainty

**Files:**
- Create: `docs/notebooks/17-filtering-and-forecasting-with-uncertainty.ipynb`
- Modify: `docs/notebooks/index.md` (grid card + toctree line)

**Content.** A methods notebook that shows two screamer operators that carry uncertainty: `KalmanFilter` (recovering a noisy latent signal) and `BayesianRegression` (online forecasting with calibrated intervals). Use synthetic data with KNOWN ground truth so the estimate can be shown against the truth.

Cell outline (markdown + code cells, matplotlib):
1. (md) H1 title + one plain sentence on what the notebook shows.
2. (code) setup: imports (numpy, matplotlib.pyplot, `from screamer import KalmanFilter, BayesianRegression`), `rng = np.random.default_rng(0)`, a small plotting helper.
3. (md) "Recovering a latent signal with the Kalman filter" - one paragraph: a smooth true signal is observed through noise; the filter tracks the latent level online.
4. (code) build a smooth true signal (e.g. a slow sine or a gentle random walk `true = np.cumsum(0.02*rng.standard_normal(N))` or a sine), add observation noise `obs = true + sigma*rng.standard_normal(N)`, run `KalmanFilter(process_var=..., observation_var=...)(obs)`, plot true vs obs vs filtered. Read KalmanFilter.md for exact args.
5. (md) one paragraph on the process_var/observation_var trade-off (more observation_var -> smoother/laggier; more process_var -> more responsive). Keep it short, no proofs.
6. (code) show the trade-off by overlaying two filter settings (responsive vs smooth) on the same observations.
7. (md) "Online forecasting with BayesianRegression" - one paragraph: it predicts the next value from a feature and reports a predictive mean and standard deviation, updated each step.
8. (code) build a linear relationship with known slope/intercept and noise, feed `BayesianRegression(...)(x, y)` (confirm input arity + arg names from BayesianRegression.md), unpack the 4 output columns `[slope, intercept, pred_mean, pred_std]`, plot the predictive mean with a +/- 2*pred_std band against the actual y.
9. (md) one paragraph reading the plot: the band is wide early (little data) and tightens as evidence accumulates; the recovered slope converges to the true slope.
10. (code) plot the recovered slope converging to the true slope over the stream.
End the notebook here (NO summary cell).

**Steps:**
- [ ] Read KalmanFilter.md and BayesianRegression.md frontmatter for exact constructor args, input arity, and output layout.
- [ ] Author the notebook (nbformat or valid ipynb JSON), matplotlib only, seeded synthetic data, following the style constraints (no roadmap opener, no summary, no proofs, no em-dash).
- [ ] Verify execution: `poetry run jupyter nbconvert --to notebook --execute --ExecutePreprocessor.timeout=120 docs/notebooks/17-filtering-and-forecasting-with-uncertainty.ipynb --stdout > /dev/null` returns zero errors. Then clear outputs (commit with no stored outputs).
- [ ] Add the grid card + toctree line to `docs/notebooks/index.md` (place in a sensible section, e.g. near the forecasting/statistics group).
- [ ] Commit: `docs(notebooks): add filtering and forecasting with uncertainty (nb 17)`

---

### Task 2: Notebook 18 - Pairs trading and mean reversion

**Files:**
- Create: `docs/notebooks/18-pairs-and-mean-reversion.ipynb`
- Modify: `docs/notebooks/index.md` (grid card + toctree line)

**Content.** A worked example of the pairs/regression family: `RollingBeta`, `RollingSpread`, `RollingAlpha`, `RollingOU`, on a synthetic cointegrated pair (two price series sharing a common factor plus a mean-reverting spread).

Cell outline:
1. (md) H1 + one plain sentence.
2. (code) setup: imports (`from screamer import RollingBeta, RollingSpread, RollingAlpha, RollingOU`), `rng = np.random.default_rng(0)`, build a synthetic pair: a common random-walk factor `f`, asset A = `a0 + beta_true*f + noise`, asset B = `f + noise`, with a deliberately mean-reverting spread. Plot the two price series.
3. (md) "Hedge ratio with RollingBeta" - one paragraph.
4. (code) `RollingBeta(window_size=...)(a, b)` (confirm arg names + input order from RollingBeta.md), plot the estimated beta vs the true value.
5. (md) "The spread and its drift" - one paragraph on RollingSpread (the residual after hedging) and RollingAlpha (the intercept/drift).
6. (code) `RollingSpread(...)` and `RollingAlpha(...)`, plot the spread; mark its mean.
7. (md) "Mean reversion with RollingOU" - one paragraph: fits an Ornstein-Uhlenbeck process to the spread and reports mean-reversion parameters; use the string `output=` modes.
8. (code) `RollingOU(..., output="mrr")` (and/or "mean"/"std"; confirm modes from RollingOU.md), plot a mean-reversion signal or the fitted mean/band over the spread.
9. (md) one short paragraph reading the result (the spread reverts to its mean; the OU half-life/speed quantifies how fast). No summary cell.

**Steps:**
- [ ] Read RollingBeta.md, RollingSpread.md, RollingAlpha.md, RollingOU.md for exact constructor args, input arity (2-input pair ops), and RollingOU output-mode strings.
- [ ] Author the notebook, matplotlib only, seeded synthetic cointegrated pair, style-compliant.
- [ ] Verify execution via nbconvert (as Task 1), zero errors, then clear outputs.
- [ ] Add grid card + toctree line to `docs/notebooks/index.md`.
- [ ] Commit: `docs(notebooks): add pairs trading and mean reversion (nb 18)`

---

### Task 3: Extend notebook 04 (signal processing) with despiker and filter coverage

**Files:**
- Modify: `docs/notebooks/04-signal-processing.ipynb`

**Content.** Notebook 04 currently demonstrates only `ImpulseClip` among despikers and only the low-pass `Butter` / `MovingAverage`. Add the missing family members without disrupting the existing flow:
- Add `Hampel` and `RollingSigmaClip` alongside the existing `ImpulseClip` despiker section: on a signal with injected spikes, compare the three despikers (all using their default STRING output mode - Hampel/ImpulseClip/RollingSigmaClip gained string modes in the API-hardening pass; use `output="..."` strings, never integers). One short paragraph + one comparison plot.
- Add a non-lowpass Butterworth example: `ButterHighpass` (or `ButterBandpass`) on a signal with a slow trend plus a target band, showing the complementary role to the existing low-pass. One short paragraph + one plot. Read the relevant docs/functions_signal page for exact args (order, cutoff_freq / low_cutoff+high_cutoff, fs).
- Update the notebook's final "choosing a filter" table to mention the high-pass/band-pass Butterworth variants and the despiker family (Hampel, RollingSigmaClip, ImpulseClip). Keep it a plain reference table, no slogans.

Keep matplotlib, keep the existing `rng`/`show()` setup, keep the notebook executing under the timeout. Do not add a summary cell or any batch-vs-stream / allclose content.

**Steps:**
- [ ] Read Hampel.md, RollingSigmaClip.md, and the ButterHighpass/ButterBandpass pages for exact constructor args and output-mode strings.
- [ ] Add the despiker-comparison cells and the non-lowpass Butter cells; update the choosing-a-filter table. Add the new operators to the setup-cell import.
- [ ] Verify execution via nbconvert, zero errors. Keep outputs cleared (notebook 04 is already output-free).
- [ ] Commit: `docs(notebooks): extend signal processing with Hampel, RollingSigmaClip, and non-lowpass Butterworth`

---

### Task 4: The operator-chooser guide

**Files:**
- Create: `docs/choosing_an_operator.md`
- Modify: `docs/index.rst` (add the page to a guide toctree)

**Content.** A cross-catalog `kind: guide` page answering "I know my task; which operator do I use?". Organize by task, each a short subsection with a decision table (task/intent -> operator(s) -> one-line note). Cover the main families a newcomer reaches for. Suggested sections:
- Smooth or denoise a series (RollingMean, EwMean, RollingMedian, WMA/DEMA/TEMA/TRIMA/HullMA, MovingAverage, Butter, KalmanFilter).
- Remove outliers / despike (Hampel, RollingSigmaClip, ImpulseClip, Clip).
- Measure dispersion or volatility (RollingStd/Var, RollingIqr, RollingRange, RollingMad, Ew volatility estimators).
- Fit a local trend or slope (RollingPoly1/2, RollingSlope, ExpandingSlope, TRIX for momentum).
- Relate two series (RollingCorr, RollingBeta, RollingCov, RollingSpread, RollingAlpha, EwBeta/EwCorr/EwCov).
- Mean reversion and forecasting (RollingOU, BayesianRegression, KalmanFilter).
- Whole-history (expanding) vs trailing-window (rolling) - one short paragraph on choosing the family.
- Align or combine streams (CombineLatest, Merge, Resample, Dropna, Filter, Select) - point to the multistream guide.
- Backtest a strategy - point to `choosing_a_backtest_engine`.

Frontmatter (guide shape):
```yaml
---
name: choosing_an_operator
title: Choosing an operator
kind: guide
short: A task-oriented map from what you want to do to the operator that does it.
topics:
- getting-started
---
```
Lead with one plain sentence. Use markdown tables like the backtest guide. Every operator named must exist (verify against the catalog / `screamer.__all__` or the docs/functions_* pages). End with `<!-- HELP_END -->`. No code blocks needed. Cross-link to relevant guides and notebooks with normal Markdown links where useful.

**Steps:**
- [ ] Draft the guide, verifying every operator name against the actual catalog (grep docs/functions_*/ or `poetry run python -c "import screamer; print(sorted(screamer.__all__))"`). Do not invent operator names.
- [ ] Register it in `docs/index.rst` (add `choosing_an_operator` to the appropriate guide toctree - place near usage / polymorphic_api).
- [ ] Verify it builds (it is a plain Markdown page; a full make docs is Task 6, but confirm the toctree entry matches the filename).
- [ ] Commit: `docs(guide): add the choosing-an-operator task map`

---

### Task 5: Installation and prerequisites page

**Files:**
- Create: `docs/installation.md`
- Modify: `docs/index.rst` (toctree + replace the bare `pip install` block with a link/expanded text), `README.md` (expand the install section)

**Content.** A short installation/prerequisites page stating:
- Python 3.11 or newer (the `requires-python = ">=3.11"` floor; classifiers list 3.11-3.14).
- `pip install screamer` installs the compiled package; the sole runtime dependency is `pybind11` (handled by the wheel).
- To run the example notebooks you also need numpy (and matplotlib/pandas for some); `pip install "screamer[docs]"` or install them directly.
- A dev/from-source note: clone, `pip install -e .` or the project's `make` targets, and the `[test]` extra (pytest, numpy>=2.0, scipy, pandas, PyYAML) for running the suite. Keep this accurate to pyproject.toml; do not overstate.

Frontmatter: a top-level page does not need `kind: guide` unless it should appear in the help registry; match how usage.md / nan_and_warmup.md set their frontmatter (open one to copy the minimal frontmatter shape). Lead with the point, no roadmap opener.

Also: in `README.md`, expand the one-line install into "Python 3.11+; `pip install screamer`" with a one-line prerequisites note. In `docs/index.rst`, keep a short install snippet but link to the new page for details, and add `installation` to the toctree.

**Steps:**
- [ ] Read pyproject.toml to confirm the exact Python floor, runtime dep, and extras. Read usage.md's frontmatter for the minimal top-level-page frontmatter shape.
- [ ] Write `docs/installation.md`; update README.md and docs/index.rst.
- [ ] Register `installation` in the docs/index.rst toctree.
- [ ] Commit: `docs: add installation and prerequisites page`

---

### Task 6: Full docs render and verification

**Files:** none new; fix any notebook/page from Tasks 1-5 that fails to build.

**Steps:**
- [ ] Run `make docs`. Expected: build completes; notebooks 17, 18, and the edited 04 execute cleanly (raise-on-error is on, so any cell exception fails the build); the new guide and install pages render and are reachable from their toctrees (no "document isn't included in any toctree" warnings); the only warnings are the 2 known pre-existing sphinx_exec_code startup warnings.
- [ ] If anything fails, fix it and re-run until clean.
- [ ] Confirm the two new notebooks appear in the built HTML under `docs/_build/html/notebooks/` and the two new pages under `docs/_build/html/`.
- [ ] Commit any fixes: `docs: fix Tier 4 build issues`
